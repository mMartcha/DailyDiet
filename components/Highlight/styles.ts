import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        alignSelf:'center',
        height:100,
        borderRadius:8,
        alignItems:'center',
        justifyContent:'center',
        marginTop:20,
        flexDirection:'row'
    }
})