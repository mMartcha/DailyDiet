import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        marginHorizontal:20,
        marginTop:20,
        gap:6
    },
    input:{
        borderWidth:1,
        borderRadius:6,
        borderColor:'lightgrey',
        padding:6,
    }
})